#ifndef METADATAWRITER_H
#define METADATAWRITER_H

#include "closet.h"

class MetaDataWriter
{
public:
    MetaDataWriter(Closet clst);
    ~MetaDataWriter(Closet clst);
};

#endif // METADATAWRITER_H
